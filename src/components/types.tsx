export type Card = {
  title: string;
  price: string;
  text: string;
};

export type Benefit = {
  id: number;
  title: string;
  text: string;
};

export type Principle = {
  title: string;
  text: string;
};
