baseURL = "https://xn--18-6kca2bmbedxg.xn--p1ai/"
defaultContentLanguage = "ru"
defaultContentLanguageInSubdir = false
paginate = 9

[languages]
[languages.ru]
  languageName = "Русский"
  weight = 1

[languages.en]
  languageName = "English"
  weight = 2

[taxonomies]
  # пустой блок — отключает дефолтные таксономии Hugo (tags, categories)
  # category: используется как обычный frontmatter-параметр, не как таксономия

[build]
  writeStats = true
